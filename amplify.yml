version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Site estático - sem build step necessário"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
